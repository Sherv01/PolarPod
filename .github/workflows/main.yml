name: Workflow Using Secret

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Use the secret
        run: |
          echo "Using secret AYUSHI_API_KEY"
          echo "API Key is ${{ secrets.AYUSHI_API_KEY }}"  # Use the secret in your script (but be cautious about logging it)

      # If you need to use the secret in a script or configuration file:
      
      - name: Set up API key as environment variable
        run: |
          echo "A_API_KEY=${{ secrets.AYUSHI_API_KEY }}" >> $GITHUB_ENV  # Set the secret as an environment variable for later steps
      - name: Example usage of API_KEY
        run: |
          echo "A_API_KEY=${{ secrets.AYUSHI_API_KEY }}" > .env
          echo "The API key is stored on the environment variable"
          echo "API Key: $A_API_KEY$ 
